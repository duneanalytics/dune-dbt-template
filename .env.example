# Environment variables for Dune dbt project
# Copy this file to .env and fill in your actual values:
#   cp .env.example .env
# The .env file will be automatically picked up by uv when running commands

# Your Dune API key (get it from https://dune.com/settings/api)
DUNE_API_KEY=your_dune_api_key_here

# Your Dune team name (e.g., "my_team")
DUNE_TEAM_NAME=your_team_name

# Optional: Schema suffix for development environment (e.g., your name or identifier)
# This allows multiple developers to have separate schema namespaces
# Example: "john" would create schemas like "team__tmp_john"
DEV_SCHEMA_SUFFIX=dev
