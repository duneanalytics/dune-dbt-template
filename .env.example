# Environment variables for dbt template
# Copy this to .env and fill in your values
# Loaded with: source .env

# Dune API Key (required)
export DBT_TEMPLATE_API_KEY=your_api_key_here

# DEV_SCHEMA_SUFFIX: Uncomment ONE of these lines based on your workflow
# Option 1: Use a suffix (for development with custom schema)
# export DEV_SCHEMA_SUFFIX=your_suffix

# Option 2: Explicitly unset (for production or default schema)
unset DEV_SCHEMA_SUFFIX